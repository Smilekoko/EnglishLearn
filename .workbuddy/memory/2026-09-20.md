# 2026-09-20

- 用户反馈 GitHub 上 Markdown 的音频链接无法内联播放，要求改为 HTML。
- 新建 `phonetics/Vowels.html`：完整复刻 Vowels.md 内容，20 个音标均为"▶"按钮，使用原生 HTML5 Audio + 少量原生 JS 实现就地播放（无外部 CDN 依赖，GitHub Pages / 本地直接打开均可）。功能：单击播放/暂停、连续播放模式开关、顺序播放全部、停止、正在播放显示。
- README.md 更新为同时链接 HTML 版与 Markdown 源文件。
- 按用户要求调整 Vowels.html：删除例句列；列顺序改为 音标(播放按钮) → 示例单词 → 发音要点；示例单词全部标注完整音标（含重音符号），目标音素用粉色高亮（.hi 类，#d63384）。最小对立对表也加了完整音标。
- 再次调整列顺序：音标 → 发音要点 → 示例单词（完整音标）。教训：用正则批量交换列时，group 里包含 </tr> 导致行结构错乱、还跨行误匹配破坏了最小对立对表；最终用脚本按段落整体重建该表修复。以后批量改 HTML 表格列顺序，优先整段重建而非正则换列。
- 用户已删除 phonetics/Vowels.md（HTML 版为唯一元音资料），README.md 中元音条目去掉 md 链接，只保留 Vowels.html。
