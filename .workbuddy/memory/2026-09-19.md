# 2026-09-19

- 初始化身份流程（BOOTSTRAP）已启动，等待用户补充姓名/城市/称呼等信息。
- 应用户要求创建根目录 `README.md`，将 `grammar/` 下的三个语法文件（noun.md、adjective.md、adverb.md）以清晰结构（表格 + 快速入口）链接进去。
- 新建 `phonetics/Vowels.md` 元音音标大全（20 个音标：12 单元音 + 8 双元音，含发音要点、易混淆对比），并在 README.md 增加语音资料板块。
- 用 edge-tts（en-US-AriaNeural，装在 C:\Users\32916\.workbuddy\binaries\python\envs\default）为 20 个元音音标生成示例单词发音 MP3（phonetics/audio/*.mp3），并把 Vowels.md 中每个音标符号做成点击播放的超链接。注意：本会话的 Bash 工具 coreutils 损坏（ls/date 失效），需用 PowerShell 或 Glob 验证文件。
- 用户反馈要"音标本身的纯发音"而非单词发音。改用 Wikimedia Commons 的 IPA 元音官方录音（ogg→mp3，ffmpeg 来自 imageio-ffmpeg 包）：单元音直接转码，双元音由两个单元音 wav 拼接（间隔 40ms）合成，/ɜː/ 用 /ə/ 录音放慢 0.85 倍速。踩坑：Commons 文件名带连字符（Mid-central vowel.ogg 而非 Mid central vowel.ogg），md5 路径法可绕过 API 直连 upload.wikimedia.org；网络不稳需 curl 重试循环；PowerShell 工具在本会话 stdout 被吞、退出码偶发 1，验证一律走 Python 写 UTF-8 文件再 Read。最终 20 个 vowel_*.mp3 存于 phonetics/audio/，Vowels.md 全部链接已关联（含对比表的音标也可点击）。
